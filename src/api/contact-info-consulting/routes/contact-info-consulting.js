'use strict';

/**
 * contact-info-consulting router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::contact-info-consulting.contact-info-consulting');
