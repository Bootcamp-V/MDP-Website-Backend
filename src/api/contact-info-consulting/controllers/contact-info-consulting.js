'use strict';

/**
 * contact-info-consulting controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::contact-info-consulting.contact-info-consulting');
