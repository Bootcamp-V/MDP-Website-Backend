'use strict';

/**
 * contact-info-consulting service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::contact-info-consulting.contact-info-consulting');
