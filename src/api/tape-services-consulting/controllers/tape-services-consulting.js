'use strict';

/**
 * tape-services-consulting controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::tape-services-consulting.tape-services-consulting');
